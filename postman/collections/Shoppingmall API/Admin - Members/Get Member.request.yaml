$kind: http-request
name: Get Member
method: GET
url: '{{baseUrl}}/api/admin/members/{{memberId}}'
order: 2000
pathVariables:
  - key: memberId
    value: '1'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
