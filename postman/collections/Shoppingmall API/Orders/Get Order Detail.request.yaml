$kind: http-request
name: Get Order Detail
method: GET
url: '{{baseUrl}}/api/orders/{{orderId}}'
order: 3000
pathVariables:
  - key: orderId
    value: '1'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
